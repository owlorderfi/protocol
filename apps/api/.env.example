NODE_ENV=development
API_PORT=3001

# Database
DATABASE_URL=postgresql://polyorder:polyorder@localhost:5432/polyorder?schema=public

# JWT — minimum 32 chars, rotate in production
# Generate with: openssl rand -hex 32
JWT_SECRET=replace-with-long-random-string-min-32-chars
JWT_EXPIRES_IN=24h

# SIWE `domain` in the /auth/login message the wallet signs. MUST equal the
# site's browser-origin host in prod (owlorderfi.com) — otherwise security
# wallets (Rabby) red-flag the sign-in as phishing. Dev: your local host.
LOGIN_DOMAIN=owlorderfi.com

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Chain RPC (read-only, server-side)
POLYGON_RPC=https://polygon-rpc.com
AMOY_RPC=https://rpc-amoy.polygon.technology

# ─── Sentry (optional) ───────────────────────────────────────────
# Leave empty to disable telemetry (sane default for dev / local).
# Create a Node project at sentry.io → Settings → Client Keys (DSN).
SENTRY_DSN=
# Optional: lets Sentry group errors by release. Set to git SHA on
# deploy; safe to leave empty otherwise.
SENTRY_RELEASE=

# ─── Geo-block (frontend banner) ────────────────────────────────
# Country block list — mirrors the CF Custom Rule 'OFAC sanctions
# geo-block' (Tier 1). Comma-separated ISO 3166-1 alpha-2.
GEO_BLOCKED_COUNTRIES=IR,KP,CU,SY
# Sub-national CIDR file — same list uploaded to the CF IP List
# 'sanctioned_subnational' (A.4 Tier 2). Generated annually by
# ops/ops/scripts/refresh-sanctions-cidr-list.py.
GEO_BLOCKED_CIDR_FILE=/path/to/geo/cf-sanctions-subnational/cidrs-all.txt
